import { html, LitElement } from 'lit-element';

class ConfigureSchedule extends LitElement {
	render() {
		return html`
      <div>Hello from d2l-configure-schedule!</div>
    `;
	}
}

customElements.define('d2l-configure-schedule', ConfigureSchedule);
